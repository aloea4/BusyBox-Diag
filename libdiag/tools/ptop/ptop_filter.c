#include "ptop.h"
#include <string.h>

/* skeleton: currently no filtering applied */
int ptop_filter_apply(ptop_delta_result_t *delta,
                      const ptop_config_t *cfg)
{
    (void)delta;
    (void)cfg;
    return 0;
}
